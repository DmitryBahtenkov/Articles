using Autofac;
using Autofac.Core;
using Autofac.Core.Registration;

namespace AutofacApi;

public class MainModule : Module
{
    protected override void Load(ContainerBuilder builder)
    {
        base.Load(builder);
    }
}